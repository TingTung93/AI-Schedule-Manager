# Department Assignment Flow Diagrams

## C4 Model - Context Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     AI Schedule Manager                         │
│                                                                 │
│  ┌──────────────┐         ┌──────────────┐                    │
│  │   Frontend   │────────▶│  Backend API │                    │
│  │   (React)    │  HTTPS  │   (FastAPI)  │                    │
│  └──────────────┘         └───────┬──────┘                    │
│                                    │                            │
│                                    ▼                            │
│                           ┌──────────────┐                     │
│                           │  PostgreSQL  │                     │
│                           │   Database   │                     │
│                           └──────────────┘                     │
└─────────────────────────────────────────────────────────────────┘

External Systems:
- User Browser (HTTPS) → Frontend
- Admin Dashboard → Backend API
```

---

## C4 Model - Container Diagram (Department Assignment)

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Backend API Container                           │
│                                                                     │
│  ┌─────────────────┐         ┌─────────────────┐                  │
│  │  API Layer      │         │  Schema Layer   │                  │
│  │  (FastAPI)      │────────▶│  (Pydantic)     │                  │
│  │                 │         │                 │                  │
│  │ - GET /         │         │ - EmployeeCreate│                  │
│  │ - POST /        │         │ - EmployeeUpdate│                  │
│  │ - PATCH /       │         │ - Validators    │                  │
│  └────────┬────────┘         └─────────────────┘                  │
│           │                                                         │
│           ▼                                                         │
│  ┌─────────────────┐         ┌─────────────────┐                  │
│  │  Service Layer  │────────▶│  Model Layer    │                  │
│  │                 │         │  (SQLAlchemy)   │                  │
│  │ - Validation    │         │                 │                  │
│  │ - Business Logic│         │ - Employee      │                  │
│  └─────────────────┘         │ - Department    │                  │
│                              │ - Relationships │                  │
│                              └────────┬────────┘                  │
│                                       │                            │
│                                       ▼                            │
│                              ┌─────────────────┐                  │
│                              │   PostgreSQL    │                  │
│                              │    Database     │                  │
│                              └─────────────────┘                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Component Diagram - Employee Creation Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                    POST /api/employees                               │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 1: Request Validation (Pydantic Schema)                       │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  EmployeeCreate Schema                                 │         │
│  │  - Validate field types (firstName, lastName)          │         │
│  │  - Validate email format (if provided)                 │         │
│  │  - Convert empty strings to None                       │         │
│  │  - Validate departmentId format (positive int)         │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 2: Department Validation (If departmentId provided)           │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  SELECT * FROM departments WHERE id = {departmentId}   │         │
│  │                                                         │         │
│  │  IF NOT EXISTS → 404 Error                             │         │
│  │  IF department.active = false → 422 Error              │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 3: Email Validation                                           │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  IF email NOT provided:                                │         │
│  │    Generate: {firstName}.{lastName}.{uuid}@temp.com    │         │
│  │                                                         │         │
│  │  SELECT * FROM employees WHERE email = {email}         │         │
│  │  IF EXISTS → 409 Conflict Error                        │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 4: Employee Creation                                          │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  INSERT INTO employees (                               │         │
│  │    email, password_hash, first_name, last_name,        │         │
│  │    department_id, is_active                            │         │
│  │  ) VALUES (...)                                        │         │
│  │                                                         │         │
│  │  COMMIT TRANSACTION                                    │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 5: Load Department Details (If assigned)                      │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  SELECT departments.*                                  │         │
│  │  FROM departments                                      │         │
│  │  WHERE id = {employee.department_id}                   │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│  Step 6: Response Serialization                                     │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  EmployeeResponse Schema                               │         │
│  │  - Convert to camelCase                                │         │
│  │  - Include nested department object                    │         │
│  │  - Return 201 Created                                  │         │
│  └────────────────────────────────────────────────────────┘         │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Sequence Diagram - Employee Update with Department Reassignment

```
Frontend        API Router       Validator        DB Service       Database
   │                │                │                │                │
   │  PATCH /api/  │                │                │                │
   │  employees/42 │                │                │                │
   ├───────────────▶│                │                │                │
   │  {             │                │                │                │
   │   departmentId│                │                │                │
   │   : 10        │                │                │                │
   │  }            │                │                │                │
   │                │  Validate      │                │                │
   │                │  Schema        │                │                │
   │                ├───────────────▶│                │                │
   │                │                │  OK            │                │
   │                │◀───────────────┤                │                │
   │                │                │                │                │
   │                │  Find Employee │                │                │
   │                │  ID=42         │                │                │
   │                ├────────────────┼───────────────▶│                │
   │                │                │                │  SELECT *      │
   │                │                │                │  FROM employees│
   │                │                │                │  WHERE id=42   │
   │                │                │                ├───────────────▶│
   │                │                │                │                │
   │                │                │                │  [Employee]    │
   │                │                │                │◀───────────────┤
   │                │                │   Employee     │                │
   │                │◀───────────────┼────────────────┤                │
   │                │                │                │                │
   │                │  Validate Dept │                │                │
   │                │  ID=10         │                │                │
   │                ├───────────────▶│                │                │
   │                │                │  Check Exists  │                │
   │                │                ├───────────────▶│                │
   │                │                │                │  SELECT *      │
   │                │                │                │  FROM depts    │
   │                │                │                │  WHERE id=10   │
   │                │                │                ├───────────────▶│
   │                │                │                │                │
   │                │                │                │  [Department]  │
   │                │                │                │◀───────────────┤
   │                │                │   Department   │                │
   │                │                │◀───────────────┤                │
   │                │                │                │                │
   │                │                │  Check Active  │                │
   │                │                │  ✓             │                │
   │                │   Valid        │                │                │
   │                │◀───────────────┤                │                │
   │                │                │                │                │
   │                │  Update        │                │                │
   │                │  Employee      │                │                │
   │                ├────────────────┼───────────────▶│                │
   │                │                │                │  UPDATE        │
   │                │                │                │  employees SET │
   │                │                │                │  dept_id=10    │
   │                │                │                │  WHERE id=42   │
   │                │                │                ├───────────────▶│
   │                │                │                │                │
   │                │                │                │  COMMIT        │
   │                │                │                │◀───────────────┤
   │                │                │  Updated       │                │
   │                │◀───────────────┼────────────────┤                │
   │                │                │                │                │
   │                │  Load Dept     │                │                │
   │                │  Details       │                │                │
   │                ├────────────────┼───────────────▶│                │
   │                │                │                │  SELECT *      │
   │                │                │                │  FROM depts    │
   │                │                │                │  WHERE id=10   │
   │                │                │                ├───────────────▶│
   │                │                │                │  [Dept Details]│
   │                │                │                │◀───────────────┤
   │                │                │   Department   │                │
   │                │◀───────────────┼────────────────┤                │
   │                │                │                │                │
   │  200 OK        │                │                │                │
   │  {             │                │                │                │
   │   id: 42       │                │                │                │
   │   departmentId │                │                │                │
   │   : 10         │                │                │                │
   │   department:  │                │                │                │
   │   {...}        │                │                │                │
   │  }             │                │                │                │
   │◀───────────────┤                │                │                │
   │                │                │                │                │
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Employee Creation Data Flow                     │
└─────────────────────────────────────────────────────────────────────┘

Input Data                     Validation Layers              Database
┌──────────┐                  ┌────────────────┐            ┌─────────┐
│  {       │                  │  Schema Layer  │            │ Emp     │
│firstName:│─────────────────▶│  - Type check  │            │ Table   │
│ "John",  │  JSON            │  - Format check│            ├─────────┤
│lastName: │  Request         │  - Required    │            │ dept_id │
│ "Doe",   │                  │    fields      │            │   │     │
│department│                  └────────┬───────┘            │   │     │
│Id: 5     │                           │                    │   │     │
└──────────┘                           ▼                    │   │     │
                              ┌────────────────┐            │   │     │
                              │ Business Logic │            │   │     │
                              │  - Dept exists?│───────────▶│   │     │
                              │  - Dept active?│   Query    │   ▼     │
                              │  - Email unique│            │ Dept    │
                              └────────┬───────┘            │ Table   │
                                       │                    ├─────────┤
                                       ▼                    │ id: 5   │
                              ┌────────────────┐            │ active  │
                              │  Data Layer    │            │  true   │
                              │  - Create emp  │◀───────────┤         │
                              │  - Set dept_id │  FK Check  └─────────┘
                              │  - Commit      │
                              └────────┬───────┘
                                       │
                                       ▼
Output Data                   ┌────────────────┐
┌──────────┐                  │  Response      │
│  {       │◀─────────────────┤  Serialization │
│id: 42,   │  JSON            │  - Load dept   │
│firstName:│  Response        │  - camelCase   │
│ "John",  │                  │  - Nested obj  │
│department│                  └────────────────┘
│: {...}   │
└──────────┘
```

---

## Error Handling Flow

```
┌────────────────────────────────────────────────────────────────┐
│              Department Assignment Error Scenarios             │
└────────────────────────────────────────────────────────────────┘

Request                   Validation Point           Error Response
────────                  ────────────────           ──────────────

departmentId: "abc"  ──▶  Schema Validator    ──▶  400 Bad Request
                          (Type check)              "Invalid format"

departmentId: -5     ──▶  Schema Validator    ──▶  400 Bad Request
                          (Range check)             "Must be positive"

departmentId: 999    ──▶  Database Query      ──▶  404 Not Found
                          (Existence check)         "Department not found"

departmentId: 5      ──▶  Business Logic      ──▶  422 Unprocessable
(inactive)                (Active check)            "Department inactive"

email: "duplicate"   ──▶  Database Query      ──▶  409 Conflict
                          (Uniqueness check)        "Email exists"

Valid Request        ──▶  All Checks Pass     ──▶  201 Created
                                                    {employee data}
```

---

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                    Database Entity Diagram                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   departments       │
├─────────────────────┤
│ id (PK)             │◀─────────────┐
│ name (UNIQUE)       │              │
│ description         │              │ 1:N
│ parent_id (FK)      │──┐           │
│ active              │  │           │
│ settings (JSONB)    │  │           │
│ created_at          │  │           │
│ updated_at          │  │           │
└─────────────────────┘  │           │
         │               │           │
         └───────────────┘           │
         Self-referential            │
         (Hierarchy)                 │
                                     │
                                     │
┌─────────────────────┐              │
│   employees         │              │
├─────────────────────┤              │
│ id (PK)             │              │
│ email (UNIQUE)      │              │
│ password_hash       │              │
│ first_name          │              │
│ last_name           │              │
│ department_id (FK)  │──────────────┘
│ is_active           │
│ is_admin            │
│ created_at          │
│ updated_at          │
└─────────────────────┘

Constraints:
- employees.department_id REFERENCES departments.id ON DELETE SET NULL
- departments.parent_id REFERENCES departments.id (self-referential)

Indexes:
- ix_employees_department_id (department_id)
- ix_employees_dept_active (department_id, is_active)
- ix_departments_active (active)
```

---

## Validation Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                    Three-Layer Validation Model                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│  Layer 1: Schema Validation (Pydantic)                             │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  - Data type validation (int, str, email)            │          │
│  │  - Field requirement checks (required/optional)       │          │
│  │  - Format validation (positive integers)              │          │
│  │  - Custom validators (empty string → None)            │          │
│  │                                                        │          │
│  │  Responsibility: Syntactic correctness                │          │
│  └──────────────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────────┐
│  Layer 2: Business Logic Validation (Service Layer)               │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  - Department existence check                        │          │
│  │  - Department active status check                    │          │
│  │  - Email uniqueness check                            │          │
│  │  - Authorization checks (future)                     │          │
│  │                                                        │          │
│  │  Responsibility: Business rule enforcement            │          │
│  └──────────────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────────┐
│  Layer 3: Database Constraints (PostgreSQL)                        │
│  ┌──────────────────────────────────────────────────────┐          │
│  │  - Foreign key constraint (department_id)            │          │
│  │  - Unique constraint (email)                         │          │
│  │  - Check constraints (valid role)                    │          │
│  │  - Not null constraints                              │          │
│  │                                                        │          │
│  │  Responsibility: Data integrity guarantee             │          │
│  └──────────────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────────────┘

Each layer provides:
- Different level of validation granularity
- Progressive validation from syntax → semantics → integrity
- Clear error messages at appropriate abstraction level
```

---

## Performance Optimization Strategy

```
┌────────────────────────────────────────────────────────────────────┐
│              Query Optimization Progression                        │
└────────────────────────────────────────────────────────────────────┘

❌ Approach 1: Lazy Loading (N+1 Problem)
──────────────────────────────────────────
employees = SELECT * FROM employees LIMIT 100      # Query 1
for each employee:
    department = SELECT * FROM departments         # Query 2-101
                 WHERE id = employee.department_id

Total Queries: 101
Performance: ❌ Poor


✅ Approach 2: Eager Loading with selectinload
──────────────────────────────────────────────
employees = SELECT * FROM employees LIMIT 100      # Query 1

dept_ids = [emp.department_id for emp in employees]
departments = SELECT * FROM departments            # Query 2
              WHERE id IN (dept_ids)

Total Queries: 2 (1 + 1)
Performance: ✅ Good


⚡ Approach 3: Joined Load (Single Query)
──────────────────────────────────────────
result = SELECT employees.*, departments.*
         FROM employees
         LEFT JOIN departments
           ON employees.department_id = departments.id
         LIMIT 100

Total Queries: 1
Performance: ⚡ Best (for single employee)
Trade-off: Larger result set, more data transfer


📊 Strategy Selection:
─────────────────────
- List endpoints (GET /employees): Use selectinload (Approach 2)
- Detail endpoints (GET /employees/{id}): Use joinedload (Approach 3)
- Bulk operations: Use selectinload with batching
```

---

## Response Optimization Patterns

```
┌────────────────────────────────────────────────────────────────────┐
│              Response Optimization Strategies                      │
└────────────────────────────────────────────────────────────────────┘

Pattern 1: Conditional Loading
───────────────────────────────
GET /api/employees?include_department=true   ──▶  Full response
GET /api/employees?include_department=false  ──▶  Minimal response

Implementation:
if include_department:
    query = query.options(selectinload(User.department))


Pattern 2: Field Projection
────────────────────────────
List View (Summary):
{
  "id": 42,
  "firstName": "John",
  "lastName": "Doe",
  "departmentId": 5,
  "department": {
    "id": 5,
    "name": "Engineering"  # Only id and name
  }
}

Detail View (Full):
{
  "id": 42,
  "firstName": "John",
  "lastName": "Doe",
  "departmentId": 5,
  "department": {
    "id": 5,
    "name": "Engineering",
    "description": "Software Development",
    "parentId": 1,
    "settings": {...},     # Complete department object
    "active": true
  }
}


Pattern 3: Response Caching
────────────────────────────
@cache(ttl=300)  # 5 minutes
async def get_department(dept_id: int):
    # Frequently accessed departments cached
    return await db.get(Department, dept_id)


Performance Impact:
───────────────────
- Conditional loading: 30-50% reduction in response size
- Field projection: 20-40% faster serialization
- Response caching: 90%+ faster for repeated requests
```

---

## Deployment Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                    Deployment Phases                               │
└────────────────────────────────────────────────────────────────────┘

Phase 1: Database Migration (Completed)
────────────────────────────────────────
✅ Add department_id column to employees
✅ Create foreign key constraint
✅ Add indexes
✅ Test constraint behavior

Phase 2: Code Deployment (Current)
───────────────────────────────────
🔄 Deploy schema updates (Pydantic validators)
🔄 Deploy API endpoint enhancements
🔄 Deploy validation logic
🔄 Deploy error handling
🔄 Update API documentation

Phase 3: Feature Rollout
─────────────────────────
🔜 Enable department validation (feature flag)
🔜 Monitor error rates
🔜 Validate performance metrics
🔜 Gradual rollout to production

Phase 4: Optimization
──────────────────────
🔜 Enable response caching
🔜 Optimize query performance
🔜 Add monitoring dashboards
🔜 Performance tuning


Rollback Strategy:
──────────────────
if ENABLE_DEPT_VALIDATION == false:
    # Skip validation, use old behavior
    # Allows instant rollback without code deploy
```

This comprehensive diagram set provides visual representation of all major architectural components and flows for the department assignment integration.
